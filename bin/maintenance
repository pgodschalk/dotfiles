#!/usr/bin/env bash
set -uf -o pipefail

mas upgrade
brew update
brew upgrade
brew upgrade --cask
brew cleanup
brew doctor
pecl upgrade-all
composer global update
tldr --update

echo "n**** Applications to update manually ****"
brew list --cask -1 | xargs brew cask info | grep auto_updates
echo "adobe-creative-cloud (apps)"
echo "steam (games)"
echo "wipr (definitions)"
echo "zprezto-update"
